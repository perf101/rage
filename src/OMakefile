NATIVE_ENABLED=true
USE_OCAMLFIND=true
OCAMLPACKS=core,postgresql,str
OCAMLFLAGS=-g -thread -linkpkg -warn-error +a-4-6-9-27-28-29
FILES=main utils

UseCamlp4(sexplib.syntax, main utils)

OCamlProgram($(PROGRAM), $(FILES))

build: $(PROGRAM)

clean:
	rm -f $(PROGRAM) *.{cmi,cmx,o,omc,opt}
