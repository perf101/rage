NATIVE_ENABLED=true
USE_OCAMLFIND=true
OCAMLPACKS=core,postgresql,str
OCAMLFLAGS=-g -thread -linkpkg
FILES=main utils

UseCamlp4(sexplib.syntax, main utils)

OCamlProgram($(PROGRAM), $(FILES))

build: $(PROGRAM)

clean:
	rm -f $(PROGRAM) *.{cmi,cmx,o,omc,opt}
